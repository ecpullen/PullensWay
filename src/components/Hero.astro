---
import { withBase } from "../lib/paths";

interface Props {
  eyebrow: string;
  title: string;
  intro: string;
}

const { eyebrow, title, intro } = Astro.props;
---

<section class="hero">
  <div class="shell hero__grid">
    <div class="hero__content">
      <p class="eyebrow">{eyebrow}</p>
      <h1>{title}</h1>
      <p class="hero__intro">{intro}</p>
      <div class="hero__actions">
        <a class="button button--primary" href={withBase("/guide")}>Explore trips</a>
        <a class="button button--ghost" href={withBase("/contact")}>Ask about availability</a>
      </div>
    </div>

    <div class="hero__panel">
      <div class="hero-card">
        <p class="hero-card__label">Trip types this site can showcase</p>
        <ul>
          <li>Guided fishing and trolling runs</li>
          <li>Side-by-side trail rides and boating trips</li>
          <li>Canoe outings and seasonal ice fishing</li>
        </ul>
      </div>
      <div
        class="hero-landscape"
        style={`background-image: linear-gradient(180deg, rgba(255, 247, 232, 0.3), rgba(255, 247, 232, 0)), url('${withBase("/images/hero-landscape.svg")}')`}
        aria-hidden="true"></div>
    </div>
  </div>
</section>
